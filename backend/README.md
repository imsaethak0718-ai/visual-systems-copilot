# FastAPI Backend

This backend provides placeholder endpoints for:

- `/upload`
- `/analyze`
- `/chat`
- `/documentation`

## Run locally

```bash
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload
```
